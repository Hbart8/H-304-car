# Menu Page Guide

## 1. 按键定义

当前菜单按键映射是：

- `KEY1 = UP`
- `KEY2 = DOWN`
- `KEY3 = ENTER`
- `KEY4 = BACK`

普通菜单里：

- `KEY1` 上移
- `KEY2` 下移
- `KEY3` 进入
- `KEY4` 返回

进入功能页面后：

- `KEY3` 退出当前功能页
- `KEY4` 退出当前功能页

特殊页：

- 在 `Sensor Data` 页面里，`KEY1` 会执行一次里程/航向清零


## 2. 主菜单结构

主菜单现在是：

```text
1. System Run
2. Motor Test
3. Sensor Data
4. Settings
5. About
```


## 3. 1. System Run

子菜单有：

```text
Run Mode A
Run Mode B
Straight Test
Back
```

### Run Mode A

用途：

- 任务层演示流程
- 先直行，再进入巡线

适合：

- 看任务编排链路是否能正常跑
- 看行为层切换是否稳定

不适合：

- 单独调直行参数


### Run Mode B

用途：

- 任务层演示流程
- 先转向，再直行

适合：

- 看转向和直行串起来是否正常

不适合：

- 单独做速度或直行标定


### Straight Test

这是目前最重要的调参页面。

用途：

- 调直行
- 看偏左还是偏右
- 看实际速度和输出占空比

页面显示：

`H`
- 当前航向角

`Y`
- 当前角速度

`V`
- 左右实际速度
- 当前显示值做了 `/10` 缩放，便于 OLED 观察

`O`
- 左右输出占空比

适合：

- 调 `APP_STRAIGHT_SIDE_BIAS_MMPS`
- 调 `APP_STRAIGHT_TEST_SPEED_MMPS`

退出：

- `KEY3`
- `KEY4`


## 4. 2. Motor Test

子菜单有：

```text
M1 Speed CL
M2 Spin OL
Turn L90
Turn R90
Back
```

### M1 Speed CL

用途：

- 电机闭环速度测试
- 看四个轮子闭环后是否基本一致

适合：

- 查编码器测速
- 查闭环速度环是否稳定
- 查某个轮子是不是明显偏快或偏慢

不适合：

- 直接拿来调整整车直行

因为：

- 它主要是单纯速度闭环调试
- 不是完整的“航向锁定直行”页面


### M2 Spin OL

用途：

- 电机开环测试
- 看原始驱动输出和方向

适合：

- 查电机方向
- 查开环驱动是否正常
- 查基础占空比输出

不适合：

- 用来判断直行是否正常


### Turn L90

用途：

- 左转 90 度测试

适合：

- 测转向是否能收住
- 测陀螺仪和转向闭环是否工作


### Turn R90

用途：

- 右转 90 度测试

适合：

- 和左转 90 度配合做对比


## 5. 3. Sensor Data

这是传感器/里程调试页。

页面显示：

- `A / B / C / D`
  - 四个轮子的编码器累计计数

- `S`
  - 车体累计里程

- `H`
  - 编码器估算航向

适合：

- 查编码器计数是否正常
- 查里程方向是否正常
- 查左右轮累计差异

特殊操作：

- 在这个页面按 `KEY1`
  - 会清零里程和航向

退出：

- `KEY3`
- `KEY4`


## 6. 4. Settings

当前是信息页，不是完整参数设置页。

主要显示：

- 调度周期信息
- 调试串口信息
- 陀螺仪接口信息

适合：

- 快速确认当前软件配置是不是你想要的那版


## 7. 5. About

当前也是信息页。

主要显示：

- 工程分层风格
- 非阻塞架构说明
- 工程定位信息


## 8. 什么时候看哪个页面

如果你想查：

编码器是否正常
- 先看 `Sensor Data`

四轮闭环速度是否一致
- 看 `M1 Speed CL`

电机方向或开环输出
- 看 `M2 Spin OL`

整车会不会跑偏
- 看 `Straight Test`

90 度转向是否能收住
- 看 `Turn L90 / Turn R90`


## 9. 当前最常用的调试顺序

推荐顺序：

1. `Sensor Data`
   - 看编码器计数是否正常

2. `M1 Speed CL`
   - 看闭环速度是否大致一致

3. `Straight Test`
   - 调直行补偿

4. `Turn L90 / Turn R90`
   - 调转向效果

5. `Run Mode A / Run Mode B`
   - 联调整体任务流程


## 10. 当前最关键的一页

如果只看一页，优先看：

`Straight Test`

因为：

- 它最接近比赛里真正要用的“直行控制”
- 也是当前最直接反映整车是否偏航的页面

