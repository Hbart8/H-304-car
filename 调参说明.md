# 小车调参说明

说明：

- 这份文档按当前工程状态整理。
- 行号基于现在这版代码，后面如果继续增删代码，行号可能会有少量变化。
- 你以后调参时，优先改 `app/ARCH/config/app_config.h` 里的宏；只有行为逻辑或衔接方式不对时，才去改 `task_service.c`、`motion_control.c`、`hal_chassis.c`。

## 0. 先看这张速查表

| 现象 | 优先改的参数/逻辑 | 文件 | 行号 |
| --- | --- | --- | --- |
| 直线偏左 | `APP_STRAIGHT_SIDE_BIAS_MMPS` 调大 | `app/ARCH/config/app_config.h` | 150 |
| 直线偏右 | `APP_STRAIGHT_SIDE_BIAS_MMPS` 调小 | `app/ARCH/config/app_config.h` | 150 |
| 第一段距离偏长 | `APP_ROUTE_SEGMENT1_MM` 调小 | `app/ARCH/config/app_config.h` | 129 |
| 第一段距离偏短 | `APP_ROUTE_SEGMENT1_MM` 调大 | `app/ARCH/config/app_config.h` | 129 |
| 第二段/第三段距离不对 | `APP_ROUTE_SEGMENT2_MM` / `APP_ROUTE_SEGMENT3_MM` | `app/ARCH/config/app_config.h` | 130 / 131 |
| 90° 偏大 | `APP_ROUTE_TURN_ANGLE_DEG10` 调小 | `app/ARCH/config/app_config.h` | 132 |
| 90° 偏小 | `APP_ROUTE_TURN_ANGLE_DEG10` 调大 | `app/ARCH/config/app_config.h` | 132 |
| 直线接 90° 不连贯 | `APP_ROUTE_TURN_ENTRY_LEAD_MM` 先调大 | `app/ARCH/config/app_config.h` | 133 |
| 提前入弯太多，距离被吃掉 | `APP_ROUTE_TURN_ENTRY_LEAD_MM` 调小 | `app/ARCH/config/app_config.h` | 133 |
| 转完 90° 后停一下才走 | `APP_TURN_EXIT_BOOST_MMPS` / `APP_TURN_EXIT_BOOST_TICKS` 调大；必要时放宽 `APP_ROUTE_TURN_DONE_*` | `app/ARCH/config/app_config.h` | 135 / 136 / 140-146 |
| 出弯过冲，第二段一出来太猛 | `APP_TURN_EXIT_BOOST_MMPS` 或 `APP_TURN_EXIT_BOOST_TICKS` 调小 | `app/ARCH/config/app_config.h` | 135 / 136 |
| 转弯最后卡住不动 | `APP_ROUTE_TURN_STALL_*` 放宽一点 | `app/ARCH/config/app_config.h` | 143-146 |
| 转弯本身太慢、像分段 | `APP_TURN_DIRECT_DUTY_KP`、`APP_TURN_DIRECT_MAX_WHEEL_DUTY` | `app/ARCH/config/app_config.h` | 195-197 |
| 转弯太冲、太猛、容易过头 | `APP_TURN_DIRECT_DUTY_KP` 调小，`APP_TURN_DIRECT_YAW_DAMP` 调大 | `app/ARCH/config/app_config.h` | 195-197 |
| 直线末段太早减速 | `APP_DISTANCE_APPROACH_BRAKE_MM` 调小 | `app/ARCH/config/app_config.h` | 147 |
| 直线末段太肉 | `APP_DISTANCE_APPROACH_MIN_SPEED_MMPS` 调大 | `app/ARCH/config/app_config.h` | 148 |
| 到点后还往前顶 | `APP_DISTANCE_STOP_FREEZE_ERR_MM` 调大，或 `APP_DISTANCE_APPROACH_MIN_SPEED_MMPS` 调小 | `app/ARCH/config/app_config.h` | 149 / 148 |
| 电机切正反向像愣一下 | `APP_MOTOR_OUTPUT_SLEW_REVERSE_STEP` 调大 | `app/ARCH/config/app_config.h` | 102 |
| 整体输出变化太冲 | `APP_MOTOR_OUTPUT_SLEW_STEP` / `APP_MOTOR_OUTPUT_SLEW_REVERSE_STEP` 调小 | `app/ARCH/config/app_config.h` | 101 / 102 |
| 某一个轮子总比其他轮子快/慢 | `APP_MOTOR_*_DUTY_SCALE` / `TRIM` / `START_DUTY` | `app/ARCH/config/app_config.h` | 108-121 |
| 无线串口乱码、刷太快 | `APP_SCHED_PERIOD_DEBUG_MS` 调大 | `app/ARCH/config/app_config.h` | 18 |
| 上电刚开始串口有乱码 | `APP_DEBUG_STARTUP_MUTE_MS` 调大 | `app/ARCH/config/app_config.h` | 19 |

### 逻辑文件怎么找

- 如果你只是调“数值大小”，基本都在 `app/ARCH/config/app_config.h`。
- 如果你觉得“动作切换方式不对”，去看 `task_service.c`。
- 如果你觉得“控制输出方式不对”，去看 `motion_control.c`。
- 如果你觉得“电机输出响应太拖”或“单轮补偿没生效”，去看 `hal_chassis.c`。

## 1. 路线基础参数

文件：

- `app/ARCH/config/app_config.h`

当前关键行：

- 第 128 行：`APP_ROUTE_TEST_SPEED_MMPS`
- 第 129 行：`APP_ROUTE_SEGMENT1_MM`
- 第 130 行：`APP_ROUTE_SEGMENT2_MM`
- 第 131 行：`APP_ROUTE_SEGMENT3_MM`
- 第 132 行：`APP_ROUTE_TURN_ANGLE_DEG10`
- 第 133 行：`APP_ROUTE_TURN_ENTRY_LEAD_MM`

怎么调：

- 第一段距离短了，就把 `APP_ROUTE_SEGMENT1_MM` 调大。
- 第一段距离长了，就把 `APP_ROUTE_SEGMENT1_MM` 调小。
- 第二段或第三段距离不对，就改 `APP_ROUTE_SEGMENT2_MM` / `APP_ROUTE_SEGMENT3_MM`。
- 直线整体太慢，就把 `APP_ROUTE_TEST_SPEED_MMPS` 调大。
- 直线整体太快，就把 `APP_ROUTE_TEST_SPEED_MMPS` 调小。
- 90° 偏大，就把 `APP_ROUTE_TURN_ANGLE_DEG10` 调小。
- 90° 偏小，就把 `APP_ROUTE_TURN_ANGLE_DEG10` 调大。
- 直线接 90° 不连贯、老像要先收一下再转，就把 `APP_ROUTE_TURN_ENTRY_LEAD_MM` 调大。
- 如果提前量太大导致第一段距离变短很多，就把 `APP_ROUTE_TURN_ENTRY_LEAD_MM` 调小。

当前值：

- `APP_ROUTE_TEST_SPEED_MMPS = 320`
- `APP_ROUTE_SEGMENT1_MM = 2400`
- `APP_ROUTE_SEGMENT2_MM = 500`
- `APP_ROUTE_SEGMENT3_MM = 500`
- `APP_ROUTE_TURN_ANGLE_DEG10 = 442`
- `APP_ROUTE_TURN_ENTRY_LEAD_MM = 160`

## 2. 转弯结束放行条件

文件：

- `app/ARCH/config/app_config.h`

当前关键行：

- 第 140 行：`APP_ROUTE_TURN_DONE_HEADING_ERR_DEG10`
- 第 141 行：`APP_ROUTE_TURN_DONE_YAW_ABS_MDPS`
- 第 142 行：`APP_ROUTE_TURN_DONE_HOLD_CYCLES`
- 第 143-146 行：`APP_ROUTE_TURN_STALL_*`

怎么调：

- 转完 90° 后老要顿一下再走，优先调 `APP_ROUTE_TURN_DONE_YAW_ABS_MDPS`。
- 想让它更早放行到直线，就把 `APP_ROUTE_TURN_DONE_YAW_ABS_MDPS` 调大，或者把 `APP_ROUTE_TURN_DONE_HOLD_CYCLES` 调小。
- 如果放得太早，出现第二个 90° 后卡住、抖动、角度发飘，就把 `APP_ROUTE_TURN_DONE_YAW_ABS_MDPS` 调小，或者把 `APP_ROUTE_TURN_DONE_HOLD_CYCLES` 调大。
- 如果转弯最后差一点老卡住不动，就看 `APP_ROUTE_TURN_STALL_*`。
- 想让“接近目标但基本不再变化”时直接判完成，就把 `APP_ROUTE_TURN_STALL_HOLD_CYCLES` 调小，或者把 `APP_ROUTE_TURN_STALL_YAW_ABS_MDPS` 调大一点。

当前值：

- `APP_ROUTE_TURN_DONE_HEADING_ERR_DEG10 = 20`
- `APP_ROUTE_TURN_DONE_YAW_ABS_MDPS = 320`
- `APP_ROUTE_TURN_DONE_HOLD_CYCLES = 2`
- `APP_ROUTE_TURN_STALL_HEADING_ERR_DEG10 = 25`
- `APP_ROUTE_TURN_STALL_YAW_ABS_MDPS = 40`
- `APP_ROUTE_TURN_STALL_DELTA_DEG10 = 1`
- `APP_ROUTE_TURN_STALL_HOLD_CYCLES = 8`

## 3. 转弯后接直线的“出弯前冲”

文件：

- `app/ARCH/config/app_config.h`
- `app/ARCH/control/motion_control.c`

当前关键行：

- `app/ARCH/config/app_config.h`
  - 第 135 行：`APP_TURN_EXIT_BOOST_MMPS`
  - 第 136 行：`APP_TURN_EXIT_BOOST_TICKS`
- `app/ARCH/control/motion_control.c`
  - 第 483-490 行：检测 `TURN -> STRAIGHT` 切换时启动 boost
  - 第 578-585 行：真正给直线一个最小前冲速度

怎么调：

- 如果“第一个 90° 后到第二段直线”不连贯，“第二个 90° 后到第三段直线”也不连贯，就调这两个。
- 想让出弯更顺、更愿意直接往前冲，就把 `APP_TURN_EXIT_BOOST_MMPS` 调大，或者把 `APP_TURN_EXIT_BOOST_TICKS` 调大。
- 如果第二段直线一出来有明显过冲，就把 `APP_TURN_EXIT_BOOST_MMPS` 调小，或者把 `APP_TURN_EXIT_BOOST_TICKS` 调小。

当前值：

- `APP_TURN_EXIT_BOOST_MMPS = 300`
- `APP_TURN_EXIT_BOOST_TICKS = 12`

## 4. 直线末段减速

文件：

- `app/ARCH/config/app_config.h`
- `app/ARCH/control/motion_control.c`

当前关键行：

- `app/ARCH/config/app_config.h`
  - 第 147 行：`APP_DISTANCE_APPROACH_BRAKE_MM`
  - 第 148 行：`APP_DISTANCE_APPROACH_MIN_SPEED_MMPS`
  - 第 149 行：`APP_DISTANCE_STOP_FREEZE_ERR_MM`
- `app/ARCH/control/motion_control.c`
  - 第 538-556 行：距离环的末段减速逻辑

怎么调：

- 如果直线末段太早收油，看起来慢吞吞，就把 `APP_DISTANCE_APPROACH_BRAKE_MM` 调小。
- 如果末段虽然减速了，但减得太肉，就把 `APP_DISTANCE_APPROACH_MIN_SPEED_MMPS` 调大。
- 如果到点后还往前顶得太多，就把 `APP_DISTANCE_STOP_FREEZE_ERR_MM` 调大一点，或者把 `APP_DISTANCE_APPROACH_MIN_SPEED_MMPS` 调小一点。

当前值：

- `APP_DISTANCE_APPROACH_BRAKE_MM = 120`
- `APP_DISTANCE_APPROACH_MIN_SPEED_MMPS = 150`
- `APP_DISTANCE_STOP_FREEZE_ERR_MM = 20`

## 5. 直线偏左 / 偏右

文件：

- `app/ARCH/config/app_config.h`

当前关键行：

- 第 150 行：`APP_STRAIGHT_SIDE_BIAS_MMPS`

怎么调：

- 直线偏左，就把 `APP_STRAIGHT_SIDE_BIAS_MMPS` 调大。
- 直线偏右，就把 `APP_STRAIGHT_SIDE_BIAS_MMPS` 调小。

原因：

- 在 `motion_control.c` 里，直线状态下会做：
  - 左轮目标速度 `+ bias`
  - 右轮目标速度 `- bias`
- 所以 bias 变大，会把车往右拉回来；bias 变小，会把车往左放一点。

当前值：

- `APP_STRAIGHT_SIDE_BIAS_MMPS = 1.3f`

## 6. 纯转向本身快不快、猛不猛

文件：

- `app/ARCH/config/app_config.h`
- `app/ARCH/control/motion_control.c`

当前关键行：

- `app/ARCH/config/app_config.h`
  - 第 187-193 行附近：`APP_TURN_ASSIST_*`、`APP_TURN_DIRECT_*`
- `app/ARCH/control/motion_control.c`
  - 第 300-340 行：`MotionControl_RunTurnHeadingDutyPath`

怎么调：

- 转弯太慢、太肉，就把 `APP_TURN_DIRECT_DUTY_KP` 调大，或者把 `APP_TURN_DIRECT_MAX_WHEEL_DUTY` 调大。
- 转弯太冲、太猛，就把 `APP_TURN_DIRECT_DUTY_KP` 调小，或者把 `APP_TURN_DIRECT_YAW_DAMP` 调大。
- 接近目标时老拧不过去，就把 `APP_TURN_ASSIST_SOFT_WHEEL_DUTY` 调大。
- 接近目标时抖得厉害，就把 `APP_TURN_ASSIST_SOFT_WHEEL_DUTY` 调小，或者把 `APP_TURN_DIRECT_YAW_DAMP` 调大。

## 7. 直线接转弯不连贯

文件：

- `app/ARCH/task/task_service.c`

当前关键行：

- 第 157-170 行：`TaskService_IsRouteTurnEntryReady`
- 第 477-480 行：第一段直线提前切入左转
- 第 517-520 行：第二段直线提前切入右转

怎么调：

- 如果“直线 -> 90°”不连贯，就先调 `APP_ROUTE_TURN_ENTRY_LEAD_MM`。
- 如果想更早开始转弯，就把 `APP_ROUTE_TURN_ENTRY_LEAD_MM` 调大。
- 如果发现第一段距离明显变短了，就把它调小。

## 8. 电机输出切换太慢

文件：

- `app/ARCH/config/app_config.h`
- `app/ARCH/hal/hal_chassis.c`

当前关键行：

- `app/ARCH/config/app_config.h`
  - 第 101 行：`APP_MOTOR_OUTPUT_SLEW_STEP`
  - 第 102 行：`APP_MOTOR_OUTPUT_SLEW_REVERSE_STEP`
- `app/ARCH/hal/hal_chassis.c`
  - 第 286-300 行：`HalChassis_SlewDuty`

怎么调：

- 如果从直行切到转向时，电机像“先愣一下再反向”，就把 `APP_MOTOR_OUTPUT_SLEW_REVERSE_STEP` 调大。
- 如果整体输出变化太冲、太硬，就把 `APP_MOTOR_OUTPUT_SLEW_STEP` 或 `APP_MOTOR_OUTPUT_SLEW_REVERSE_STEP` 调小。

当前值：

- `APP_MOTOR_OUTPUT_SLEW_STEP = 140`
- `APP_MOTOR_OUTPUT_SLEW_REVERSE_STEP = 320`

## 9. 轮子个体补偿

文件：

- `app/ARCH/config/app_config.h`

当前关键行：

- 第 108-121 行：
  - `APP_MOTOR_*_DUTY_SCALE`
  - `APP_MOTOR_*_DUTY_TRIM`
  - `APP_MOTOR_*_START_DUTY`

怎么调：

- 如果某个轮子一直比其他轮子快，就先改对应轮子的 `DUTY_SCALE`。
- 如果某个轮子整体偏弱、起步吃力，可以给它加一点 `DUTY_TRIM` 或 `START_DUTY`。
- 这组参数适合处理“某一只轮子明显和其他三个不一样”的情况，不适合处理整车偏航。

## 10. 串口调试输出

文件：

- `app/ARCH/config/app_config.h`
- `app/ARCH/app_system.c`

当前关键行：

- `app/ARCH/config/app_config.h`
  - 第 18 行：`APP_SCHED_PERIOD_DEBUG_MS`
  - 第 19 行：`APP_DEBUG_STARTUP_MUTE_MS`
- `app/ARCH/app_system.c`
  - 第 15-30 行：启动静默和 `#DBG,START`

怎么调：

- 无线串口乱码、粘包严重，就把 `APP_SCHED_PERIOD_DEBUG_MS` 调大。
- 上电初期乱码，就把 `APP_DEBUG_STARTUP_MUTE_MS` 调大。

## 11. 最常用调参顺序

推荐顺序：

1. 先调 `APP_ROUTE_TURN_ENTRY_LEAD_MM`，把“直线 -> 转弯”衔接调顺。
2. 再调 `APP_TURN_EXIT_BOOST_*` 和 `APP_ROUTE_TURN_DONE_*`，把“转弯 -> 直线”衔接调顺。
3. 再调 `APP_ROUTE_TURN_ANGLE_DEG10`，把 90° 调准。
4. 最后调 `APP_STRAIGHT_SIDE_BIAS_MMPS`，把直线拉正。

不要混着乱调：

- 角度不准时，优先改 `APP_ROUTE_TURN_ANGLE_DEG10`。
- 直线偏航时，优先改 `APP_STRAIGHT_SIDE_BIAS_MMPS`。
- 衔接不连贯时，优先改 `APP_ROUTE_TURN_ENTRY_LEAD_MM`、`APP_TURN_EXIT_BOOST_*`、`APP_ROUTE_TURN_DONE_*`。

## 12. 你现在最常用的几个参数

你后面大概率最常改的是：

- `app/ARCH/config/app_config.h:129`
  - 第一段直线距离
- `app/ARCH/config/app_config.h:132`
  - 转角目标
- `app/ARCH/config/app_config.h:133`
  - 直线提前入弯距离
- `app/ARCH/config/app_config.h:135-136`
  - 出弯前冲
- `app/ARCH/config/app_config.h:140-142`
  - 出弯放行条件
- `app/ARCH/config/app_config.h:150`
  - 直线偏左/偏右
