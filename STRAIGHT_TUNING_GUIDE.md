# Straight Tuning Guide

## 1. 先看哪几个页面

这套工程里，和直行调参最相关的页面有 3 个：

`1. System Run -> Straight Test`
- 用来调直行。
- 这是最重要的页面。
- `H`：当前航向角。
- `Y`：当前角速度。
- `V`：左右实际速度显示，当前做了 `/10` 缩放，便于 OLED 观察。
- `O`：左右输出占空比。

`2. Motor Test -> M1 Speed CL`
- 用来检查闭环速度是否正常。
- 主要看四个轮子的速度/计数是不是差太多。
- 这个页面不是专门调直行的。

`2. Motor Test -> M2 Spin OL`
- 用来检查开环输出、方向、基础驱动。
- 这个页面也不是专门调直行的。


## 2. 现在直行真正调的是哪里

直行测试的核心参数在：

`app/ARCH/config/app_config.h`

当前最关键的是这两个：

```c
#define APP_STRAIGHT_TEST_SPEED_MMPS            (220)
#define APP_STRAIGHT_SIDE_BIAS_MMPS             (-6)
```

含义：

`APP_STRAIGHT_TEST_SPEED_MMPS`
- 直行测试目标速度。
- 想让车跑快一点，就调大。
- 想让车慢一点、更稳一点，就调小。

`APP_STRAIGHT_SIDE_BIAS_MMPS`
- 直行左右补偿。
- 它不是 PID 参数本身，而是一个很直接的左右速度偏置。
- 这是现在把车调到“基本直线”的关键参数。


## 3. 这个补偿怎么生效

控制层里，直行时会执行：

- 左侧目标速度 `+= bias`
- 右侧目标速度 `-= bias`

所以它的效果非常直接：

`bias` 更大
- 更偏向左侧输出大。

`bias` 更小
- 更偏向右侧输出大。

当前值是：

`APP_STRAIGHT_SIDE_BIAS_MMPS = -6`


## 4. 按现象怎么调

直接记这个规则就够了：

车右偏
- 把 `APP_STRAIGHT_SIDE_BIAS_MMPS` 调得更小一点。
- 也就是更负一点。
- 例子：`-6 -> -7`

车左偏
- 把 `APP_STRAIGHT_SIDE_BIAS_MMPS` 调大一点。
- 也就是往 `0` 方向回。
- 例子：`-6 -> -5`

基本直线
- 先不要动。
- 这时可以把这组参数当成当前硬件版本的直行标定值。

速度太慢
- 调大 `APP_STRAIGHT_TEST_SPEED_MMPS`
- 例子：`220 -> 240 -> 260`

速度太快
- 调小 `APP_STRAIGHT_TEST_SPEED_MMPS`
- 例子：`220 -> 200 -> 180`


## 5. 推荐调参步长

建议不要一次改太大。

推荐这样改：

`APP_STRAIGHT_SIDE_BIAS_MMPS`
- 每次改 `1`
- 例如：`-6 -> -5` 或 `-6 -> -7`

`APP_STRAIGHT_TEST_SPEED_MMPS`
- 每次改 `20` 或 `40`
- 例如：`220 -> 240`，`220 -> 260`


## 6. 推荐调参流程

1. 先固定速度，比如先用 `220`。
2. 进入 `Straight Test`，观察车是左偏还是右偏。
3. 只改 `APP_STRAIGHT_SIDE_BIAS_MMPS`，每次改 `1`。
4. 调到基本走直线后，再决定要不要提速。
5. 提速后如果又跑偏，继续微调 `bias`。


## 7. 看 OLED 时怎么判断

`V`
- 这是左右实际速度。
- 现在显示值是缩小后的，便于看趋势。
- 左右差很大，说明速度一致性还有问题。

`O`
- 这是左右输出占空比。
- 如果车已经基本直线，但 `O` 左右不一样，这是正常的。
- 因为控制器会自动补偿硬件差异。

不要用 `M1` 和 `M2` 直接判断直行是否正常。
- `M1` 是闭环速度测试页。
- `M2` 是开环输出测试页。
- 真正判断“会不会跑偏”，要看 `Straight Test`。


## 8. 什么时候要重新调

以下情况都建议重新调一次 `APP_STRAIGHT_SIDE_BIAS_MMPS`：

- 改了轮子
- 改了电机
- 改了驱动接线
- 改了底盘重心
- 改了硬件安装位置
- 改了电源或供电状态

因为这些变化都会让原来的左右补偿失效。


## 9. 当前这一版的建议

如果你现在这台车是“基本直线”：

- 先保留：
  - `APP_STRAIGHT_TEST_SPEED_MMPS = 220`
  - `APP_STRAIGHT_SIDE_BIAS_MMPS = -6`

后面如果你要比赛前再收一次：

- 轻微右偏：改成 `-7`
- 轻微左偏：改成 `-5`

