# 第三题衔接说明

更新时间：2026-04-24

## 1. 现在第三题的现象

当前第三题的主要衔接问题是：

- 直线衔接圆弧时会明显停一下
- 圆弧衔接直线目前效果基本可以
- 因此当前要解决的重点不是“出弧”，而是“入弧”

这和第一题、第二题的表现不同。

## 2. 第一题和第二题为什么衔接比较连贯

## 2.1 第一题的做法

第一题实现在 [task_service.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\task\task_service.c) 的 `TaskService_RunRoute2500L90_500R90_500()`：

- 函数入口：`859` 行
- 第一次直线提前切转向：`898` 行
- 第一次转向结束切直线：`919` 行
- 第二次直线提前切转向：`938` 行
- 第二次转向结束切直线：`959` 行

第一题直线切转向之所以连贯，核心不是“转得快”，而是“提前切”。

它没有等直线彻底跑完、彻底减到快停下时再转，而是在直线末段提前调用：

- `TaskService_IsRouteTurnEntryReady(...)`
  - 定义位置：`362` 行

这个函数会根据：

- 当前已走距离
- 当前段目标距离
- `APP_ROUTE_TURN_ENTRY_LEAD_MM`

来判断是不是已经进入“转向切入区”。

对应参数在 [app_config.h](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\config\app_config.h)：

- `APP_ROUTE_TURN_ENTRY_LEAD_MM`
  - 当前定义位置：`173` 行

也就是说，第一题不是跑到终点才转，而是“离终点还差一小段时就切下一段”。

## 2.2 第二题的做法

第二题实现在 [task_service.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\task\task_service.c) 的 `TaskService_RunQ2_700_Arc180_Arc180_R90_700()`：

- 函数入口：`991` 行
- 第 1 段直线切转向：`1041` 行
- 第 2 段直线切转向：`1072` 行
- 第 3 段直线切转向：`1103` 行
- 第 4 段直线切转向：`1134` 行

第二题沿用的也是同一套思路：

- 直线末段提前切入下一段角度动作
- 不等直线彻底结束

所以第一题和第二题“直线 -> 转向”的衔接能更连贯。

## 2.3 前两题的“转向 -> 直线”为什么也比较顺

前两题的“转向 -> 直线”除了任务层切得快之外，控制层还做了专门处理。

关键代码在 [motion_control.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\control\motion_control.c)：

- 命令切换检测：`498` 行
- `TURN -> STRAIGHT` 出弯加速触发：`499` 到 `504` 行
- 直线阶段使用 `turn_exit_boost_ticks`：`602` 到 `610` 行

对应参数在 [app_config.h](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\config\app_config.h)：

- `APP_TURN_EXIT_BOOST_MMPS`
  - `175` 行
- `APP_TURN_EXIT_BOOST_TICKS`
  - `176` 行

这一套东西的作用是：

- 刚从转向切回直线时
- 给直线一个短暂的出弯速度维持
- 防止转完以后立刻显得“拖一下”

所以前两题连贯，本质是两层同时在做事：

1. 任务层提前切段
2. 控制层做 `Turn -> Straight` 的出弯维持

## 3. 第三题为什么会在“直线 -> 圆弧”这里停一下

第三题实现在 [task_service.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\task\task_service.c) 的 `TaskService_RunQ3_1200_R270_500_R420_1800()`：

- 函数入口：`1178` 行
- 第一段直线结束后切第一段弧：`1213` 到 `1216` 行
- 第二段直线结束后切第二段弧：`1258` 到 `1261` 行

第三题和前两题最大的区别就在这里：

- 第一题、第二题：用 `TaskService_IsRouteTurnEntryReady(...)` 提前切段
- 第三题：用 `TaskService_IsDistanceDoneByMeasure(...)` 等直线“完成”后才切弧线

对应位置：

- `TaskService_IsDistanceDoneByMeasure(...)`
  - 定义位置：`312` 行
- 第三题第 1 段使用它：`1213` 行
- 第三题第 2 段使用它：`1258` 行

这会带来一个直接后果：

- 在距离接近目标时，距离环会主动减速
- 甚至会进入接近停下的状态
- 然后才从直线切换到圆弧

所以你看到的现象就不是“顺滑切弧”，而是：

- 直线跑完
- 车速掉下来
- 再开始进入弧线

这就是第三题“直线衔接圆弧会停下”的根因。

## 4. 第三题为什么“圆弧 -> 直线”相对还可以

第三题的“圆弧 -> 直线”在控制层已经单独补了出弧释放逻辑。

关键位置在 [motion_control.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\control\motion_control.c)：

- `ARC -> STRAIGHT` 识别：`507` 到 `513` 行
- 出弧释放窗口启用：`613` 到 `614` 行
- 释放窗口中跟随当前车头：`621` 到 `624` 行
- 释放窗口中临时关闭转向环：`725` 到 `733` 行
- 释放窗口中临时关闭直线侧偏补偿：`768` 到 `774` 行
- 释放窗口退出条件：`804` 到 `822` 行

对应参数在 [app_config.h](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\config\app_config.h)：

- `APP_ARC_EXIT_RELEASE_TICKS`
  - `177` 行
- `APP_ARC_EXIT_RELEASE_YAW_MDPS`
  - `178` 行
- `APP_ARC_EXIT_RELEASE_HOLD_TICKS`
  - `179` 行

所以第三题现在“出弧接直线”比“入弧”好，是因为控制层已经专门做了处理。

## 5. 结论

前两题衔接好的核心原因是：

- 不是跑到段末停住再切
- 而是提前切入下一段

第三题现在衔接差的核心原因是：

- 直线到圆弧没有“提前切弧”
- 仍然在等直线完全结束

因此第三题当前最该改的，不是继续改圆弧结束逻辑，而是补上：

- `Straight -> Arc` 的提前切入机制

## 6. 第三题应该怎么改

## 6.1 改动目标

把第三题从：

- 直线彻底结束
- 再启动弧线

改成：

- 直线末段提前切入弧线

这样它的行为就会更像第一题、第二题。

## 6.2 推荐修改方案

建议新增一个和第一题、第二题同风格的函数，例如：

- `TaskService_IsRouteArcEntryReady(...)`

建议位置：

- 放在 [task_service.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\task\task_service.c)
- 紧靠 `TaskService_IsRouteTurnEntryReady(...)` 附近实现

推荐逻辑：

- 输入：
  - 当前阶段已走距离
  - 当前直线目标距离
  - 当前要接入的圆弧半径
  - 当前要接入的圆弧角度
- 输出：
  - 是否应该提前切入弧线

最简单可先用固定提前量版本：

- `switch_distance_mm = distance_target_mm - APP_ROUTE_ARC_ENTRY_LEAD_MM`

也可以直接针对第三题先做成两个参数：

- `APP_Q3_ARC1_ENTRY_LEAD_MM`
- `APP_Q3_ARC2_ENTRY_LEAD_MM`

## 6.3 第三题任务层具体要改的地方

### 第一段直线切第一段弧

当前代码：

- [task_service.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\task\task_service.c) `1213` 行

当前逻辑是：

- `TaskService_IsDistanceDoneByMeasure(...)`
- 然后 `TaskService_StartArcRadiusPhase(...)`

建议改成：

- `TaskService_IsRouteArcEntryReady(...)`
- 然后直接 `TaskService_StartArcRadiusPhase(...)`

### 第二段直线切第二段弧

当前代码：

- [task_service.c](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\task\task_service.c) `1258` 行

当前逻辑同样是：

- `TaskService_IsDistanceDoneByMeasure(...)`

建议同样改成：

- `TaskService_IsRouteArcEntryReady(...)`

## 6.4 配置层建议新增的参数

建议在 [app_config.h](C:\Users\huang\Desktop\SCM\Project(2)\Project\app\ARCH\config\app_config.h) 新增：

- `APP_Q3_ARC1_ENTRY_LEAD_MM`
- `APP_Q3_ARC2_ENTRY_LEAD_MM`

或者做成通用版本：

- `APP_ROUTE_ARC_ENTRY_LEAD_MM`

如果第三题两段弧特性差异较大，我更建议用分开的两个参数，后面调起来更直接。

## 7. 代码修改优先级建议

如果你要继续收第三题衔接，建议按下面顺序改：

1. 先补 `Straight -> Arc` 的提前切入
2. 再根据实车效果调整 `APP_Q3_ARC1_ENTRY_LEAD_MM`
3. 再调整 `APP_Q3_ARC2_ENTRY_LEAD_MM`
4. 最后才去继续细调弧后直线释放参数

原因很简单：

- 现在主要停顿发生在“入弧”
- 所以先解决“入弧切换时机”
- 再去收“出弧稳定性”

## 8. 一句话总结

前两题之所以衔接顺，是因为：

- 任务层提前切下一段
- 控制层对 `Turn -> Straight` 做了出弯维持

第三题现在停顿，是因为：

- 直线到圆弧还在等“距离完成”才切
- 没有做“提前入弧”

所以第三题接下来最关键的代码修改方向就是：

- 给第三题补一套 `Straight -> Arc` 的提前切入机制
