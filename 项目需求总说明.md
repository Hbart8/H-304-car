# 2026 电赛控制组项目需求总说明

更新时间：2026-04-24

## 1. 项目背景

本工程用于参加 2026 年全国大学生电子设计大赛控制组。

工程目标：

- 采用企业级、可维护、可扩展的分层架构
- 严禁阻塞式流程控制，禁止依赖长时间卡死式延时
- 使用轻量级周期任务调度器，不使用 RTOS
- 底层硬件必须封装，屏蔽芯片差异
- 参数统一管理，便于后期现场调参与题目切换
- UI、任务、行为、控制、设备、底层必须解耦

## 2. 总体分层要求

用户明确要求采用以下逻辑分层：

- 底层：GPIO / PWM / UART / I2C / 编码器 / 电机驱动
- 设备层：陀螺仪、灰度传感器、OLED、按键、蜂鸣器、RGB
- 控制层：速度 PID、里程 PID、转向 PID、巡线 PID
- 行为层：直行、转向、巡线、走到黑线/白线
- 任务层：动作队列 + 状态机
- 调度层：周期任务调度器
- 交互层：OLED 菜单触发不同任务

同时用户又给出了更细的工程架构要求，要求统一理解为：

- 入口与总装层
- 调度层
  - 周期任务系统
  - 不是 RTOS，而是轻量级软件定时调度器
- 硬件抽象层 / 驱动层
- 设备适配层
- 控制层
  - 闭环控制核心
- 行为层
  - 动作语义封装
- 任务编排层
  - 状态机 + 动作队列
- 交互层
  - UI 菜单触发任务

## 3. 软件设计硬性要求

### 3.1 非阻塞要求

- 禁止使用阻塞式业务流程
- 可以使用软件定时器 / 周期任务调度
- 所有行为任务应拆成可轮询推进的状态机
- 菜单、显示、蜂鸣器、串口、控制循环不能互相卡死

### 3.2 解耦要求

- 上层不得直接操作芯片寄存器
- 设备层屏蔽具体芯片差异
- 控制层只依赖统一测量量，不依赖引脚细节
- 行为层只描述“怎么动”，不写具体 PWM/GPIO
- 任务层只做动作编排和阶段切换
- UI 层只发起任务，不直接写控制逻辑

### 3.3 参数管理要求

- 结构参数统一管理
- PID 参数统一管理
- 路线参数统一管理
- OLED 菜单调参入口要保留
- 后续题目切换时应尽量只改参数或任务表，不大改底层

## 4. 当前工程分层目录映射

结合当前工程目录，建议按以下方式理解：

- `app/ARCH/hal`
  - 硬件抽象层 / 驱动封装层
- `app/ARCH/device`
  - 设备适配层
- `app/ARCH/control`
  - 控制层
- `app/ARCH/behavior`
  - 行为层
- `app/ARCH/task`
  - 任务编排层
- `app/ARCH/scheduler`
  - 调度层
- `app/ARCH/interaction`
  - 交互层
- `app/MENU`
  - OLED 菜单与交互呈现
- `app/SYS`
  - 系统公共服务

## 5. 硬件与资源需求总表

## 5.1 编码器输入

用户给出的编码器资源如下：

- `PB4`  -> `ENCODER_1`
- `PB5`  -> `ENCODER_2`
- `PB6`  -> `ENCODER_3`
- `PB7`  -> `ENCODER_4`
- `PB19` -> `ENCODER_5`
- `PB18` -> `ENCODER_6`
- `PB23` -> `ENCODER_7`
- `PB13` -> `ENCODER_8`

说明：

- 编码器为中断输入
- 双边沿触发
- 用户简写中如 `A0`、`B25`，默认前面带 `P`

## 5.2 74HC595 输出扩展

- `PA25` -> `HC595_DS`
- `PA15` -> `HC595_SHCP`
- `PA14` -> `HC595_STCP`

## 5.3 PWM 电机驱动资源

- `TIMA0`
  - `PA0`  -> 左轮电机驱动通道 A
  - `PA1`  -> 左轮电机驱动通道 B
- `TIMG8`
  - `PA7`  -> 右轮电机驱动通道 A
  - `PA22` -> 右轮电机驱动通道 B

用户原始说明中使用了：

- 预分频：8
- 计数：3000

## 5.4 SPI 资源

- `SPI1`
  - `SCLK` -> `PA17`
  - `MOSI` -> `PB15`
  - `MISO` -> `PA16`

## 5.5 ADC / 循迹资源

用户给出的循迹模块资源：

- `PA27` -> ADC 输入
- `PA24` -> `74HC4051 D0`
- `PA30` -> `74HC4051 D1`
- `PA29` -> `74HC4051 D2`

说明：

- 感为科技无 MCU 循迹模块
- 通过 `74HC4051` 实现 1 路 ADC 扩展 8 路模拟输入选择

## 5.6 串口资源

- `UART_0`
  - `TX` -> `PA10`
  - `RX` -> `PA11`
  - 用途：系统调试串口 / 蓝牙模块
  - 波特率：115200
- `UART_1`
  - `TX` -> `PB2`
  - `RX` -> `PB3`
  - 用户要求：作为无线串口调试接口使用
  - 历史备注：曾被写成陀螺仪通信口，后已明确不用于陀螺仪

## 5.7 I2C 资源

用户后续明确给出的陀螺仪 I2C 连接为：

- `SDA` -> `PA26`
- `SCL` -> `PA8`

## 5.8 蜂鸣器

用户明确要求：

- 蜂鸣器必须接入系统
- 蜂鸣器不能只服务于按键提示
- 必须可在其他任务和交互逻辑中复用

## 5.9 其他设备

用户要求接入并分层管理以下设备：

- 陀螺仪
- 灰度 / 循迹传感器
- OLED
- 按键
- 蜂鸣器
- RGB

## 6. 外部资料来源

用户提供的关键资料来源如下：

- PCB 工程 PDF
  - `C:/Users/huang/Desktop/SCH_航模电池版2.0_2026-03-13.pdf`
- IMU 资料目录
  - `C:/Users/huang/Desktop/BMI270/BMI270/stm32f1xx_imu660ra/stm32f1xx_imu660ra`

说明：

- 用户已明确说明：陀螺仪资料目录中的芯片和当前板上芯片不是同一款
- 但 PCB / 接口中存在 `MPU6050 / IMU660RA` 相关接口信息
- 实际项目联调中，当前陀螺仪已按 I2C 路径处理

## 7. 小车结构与标定参数

用户明确给出的当前机械参数：

- 车轮直径：`65 mm`
- 减速比：`28:1`
- 左右轮中心距：`120 mm`

用户后续给出的四轮编码器计数标定值：

- 编码器 0：`220`
- 编码器 1：`224`
- 编码器 2：`224`
- 编码器 3：`223`

这些参数已经被要求接入里程换算和转向换算统一接口中。

## 7.1 小车硬件参数总表

这一节单独汇总“小车本体硬件参数”，用于比赛、调参、里程换算、转向换算和后续文档查阅。

### 7.1.1 底盘与传动参数

- 车型结构：四轮小车
- 驱动形式：四电机差速驱动
- 车轮直径：`65 mm`
- 车轮半径：`32.5 mm`
- 左右轮中心距：`120 mm`
- 减速比：`28:1`

### 7.1.2 编码器测量参数

当前已测到的四个轮组编码器一圈计数如下：

- 编码器 0：`220`
- 编码器 1：`224`
- 编码器 2：`224`
- 编码器 3：`223`

按当前工程中的轮位约定：

- `0`：右前轮
- `1`：右后轮
- `2`：左前轮
- `3`：左后轮

由此得到的平均值：

- 平均每圈计数：`222.75`

### 7.1.3 由机械参数推导出的关键量

根据当前参数可得到：

- 车轮周长：约 `204.20 mm`
- 底盘原地转向等效圆周长：约 `376.99 mm`

单轮每个编码器计数对应的理论位移约为：

- 右前轮：约 `0.928 mm/count`
- 右后轮：约 `0.912 mm/count`
- 左前轮：约 `0.912 mm/count`
- 左后轮：约 `0.916 mm/count`

平均每计数位移约为：

- 平均值：约 `0.917 mm/count`

### 7.1.4 已知但当前未补齐的参数

以下参数用户明确说过“暂时没有实测”或当前文档未最终确认，因此先标记为待测：

- 电机轴一圈的编码器计数：`未实测`
- 车轮一圈的编码器计数：`已通过四轮标定值替代`
- 车身前后轴距：`当前未单独记录`
- 车体外形尺寸：`当前未单独记录`

### 7.1.5 与控制直接相关的说明

- 里程换算必须基于四轮独立编码器标定值
- 转向换算必须基于 `120 mm` 左右轮中心距
- 不能把四个轮子的编码器统一视为完全相同
- 后续如果更换轮胎、减速箱、编码器安装方式，需要同步更新这一节和配置参数

## 8. PID 分工要求

用户明确要求：

- 不是一个 PID 解决所有问题
- 里程、姿态、转向、巡线、速度必须各司其职

必须保留并明确职责的 PID / 控制环：

- 速度 PID
  - 负责车轮速度闭环
- 里程 PID
  - 负责行驶距离闭环
- 姿态 PID
  - 负责直线航向保持 / 姿态约束
- 转向 PID
  - 负责原地转向或目标角度控制
- 巡线 PID
  - 负责循迹误差闭环

## 9. 行为层能力要求

用户明确要求行为层必须封装为动作语义，而不是底层动作拼接。

行为层至少应支持：

- 直行
- 转向
- 巡线
- 走到黑线
- 走到白线
- 圆弧行驶
- 支持不同题目的组合动作

后续用户还明确提出过以下需求：

- 能够随意设置圆弧角度
- 能够围绕障碍物或柱子绕行
- 支持两段相反半圆弧
- 支持不同切入方向的弧线
- 未来可扩展为：
  - 绕正右边
  - 绕正左边
  - 绕前左边
  - 绕前右边
  - 绕后左边
  - 绕后右边

## 10. 任务层要求

用户明确要求任务层必须是：

- 动作队列 + 状态机

并且强调：

- 任务切换必须非阻塞
- 直线段、转向段、弧线段应作为独立阶段推进
- 不同题目任务应可复用通用段逻辑
- 后续应进一步演进为“直线段 + 弧线段”的统一段式状态机

## 11. 调度层要求

用户明确要求：

- 使用周期任务系统
- 不是 RTOS
- 是轻量级软件定时调度器

调度层至少应承担：

- 设备轮询
- 控制周期运行
- 任务状态推进
- 交互刷新
- 调试串口输出

## 12. 交互层要求

用户明确要求：

- 通过 OLED 菜单触发任务
- 主菜单第四行 `Settings` 用于调参
- `Settings` 需要做成二级菜单
- 二级菜单第一个入口用于第一题调参

同时，交互层还需满足：

- OLED 显示稳定，不花屏、不乱码、不易卡死
- 按键响应正常
- 蜂鸣器提示不能导致菜单卡死
- 菜单行为与任务行为解耦

## 13. 串口调试要求

用户明确要求：

- 使用串口调试
- 使用无线串口调试
- 当前主要使用 `UART_1`

串口调试输出要求：

- 可输出任务状态
- 可输出直线 / 转向 / 弧线过程数据
- 可辅助判断里程、航向、角速度、目标值、输出值
- 调试格式要尽量稳定，避免 OLED 之外无法判断问题

## 14. 当前题目需求汇总

以下内容记录用户在当前工程中已经明确提出过的题目需求。

### 14.1 第一题

第一题对应 OLED 菜单中的 `Route 250LR`。

用户最终表达的路径语义为：

- 前进 `2500 mm`
- 左转 `90°`
- 前进 `500 mm`
- 右转 `90°`
- 前进 `500 mm`

要求：

- 直线与转弯衔接尽量连贯
- 不要明显停顿
- 直线稳定，侧偏小
- 角度准确

### 14.2 第二题

第二题最初讨论过圆弧绕柱方案，后面用户明确改为折线角度方案。

用户最新明确的第二题要求为：

- 直行 `700 mm`
- 左转 `45°`
- 前进 `700 mm`
- 右转 `45°`
- 前进 `700 mm`
- 左转 `45°`
- 前进 `700 mm`
- 右转 `22.5°`
- 直线 `800 mm`

说明：

- 第二题曾多次讨论圆弧版本
- 当前文档以用户后续明确替换后的折线角度版本为准

### 14.3 第三题

用户明确给出的第三题需求为：

- 前进 `1200 mm`
- 右转弧线 `270°`
- 直线 `500 mm`
- 再右转弧线 `420°`
- 直线 `1800 mm`

用户给出的弧线半径要求：

- 两段弧半径都用 `100 mm`

当前联调中，用户特别关注：

- 第一段直线正常
- 两段弧不能死转
- 弧后直线不能摇晃
- 弧线与直线切换要稳定

### 14.4 后续题目扩展要求

用户明确说过：

- 总题目数为 5 个
- 后续还会继续接入更多题目

因此要求当前架构支持：

- 任务快速扩展
- 新题目只增加任务描述和参数
- 尽量不改底层与基础控制框架

## 15. 控制与调试体验要求

这是用户在持续联调中多次反复强调的“现象级要求”，应视为硬性需求：

- OLED 不能频繁卡死
- 菜单不能乱闪
- 屏幕不能乱码
- 按键不能导致蜂鸣器一直响
- 串口必须能稳定收到有效数据
- 直线速度显示与真实运动要一致
- 不允许明显的“停一下走一下”
- 任务切换不要有 1 秒级停顿感
- 转 90° 不要分很多次慢慢补
- 弧线不能变成原地掉头或死转
- 后续直线不能被上一段弧影响得左右摇摆

## 16. 当前已确认的重要约束

### 16.1 关于陀螺仪

- 用户明确指出：陀螺仪是 I2C 通讯，不是 UART 通讯
- 项目中任何陀螺仪驱动与任务逻辑都必须按 I2C 路径设计

### 16.2 关于菜单与蜂鸣器

- 按键事件和蜂鸣器提示必须解耦
- 蜂鸣器不能因按键状态机错误而长鸣

### 16.3 关于参数调节

- 用户希望尽量通过参数解决题目差异
- 速度、偏置、角度、弧度、衔接时间都应该可调
- 后续优先通过 `Settings` 调参，不希望每次都进代码改常量

## 17. 推荐的后续工程演进方向

根据用户已经明确表达的需求，后续架构应进一步往以下方向演进：

- 建立统一的“直线段 / 原地转向段 / 弧线段”段式状态机
- 每一段单独拥有：
  - 初始化
  - 运行
  - 判停
  - 出段锁定
- 将每个题目描述为段表
- 将调参项逐步映射到 OLED `Settings`
- 将串口调试输出与任务段状态绑定

## 18. 本文档用途

本文档用于作为当前工程需求基线，覆盖：

- 用户已明确表达的架构要求
- 用户已提供的硬件资源
- 用户已确认的机械参数
- 用户已提出的控制分工要求
- 用户已定义的题目任务要求
- 用户在联调中反复强调的现象级约束

后续新增题目、新增设备或修改路线时，应优先更新本文档，再更新参数与代码。
